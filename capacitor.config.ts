import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.ahmedobozkurt.cryptoapp',
  appName: 'cryptonewapp2',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
